## 建议遵循 Ant Design Pro 的代码规范。

## 文件命名规范：

1. **React 组件文件**：使用 PascalCase（大驼峰）命名。这样可以帮助区分 React 组件与普通的 JavaScript 函数或变量。

- 示例：`LoginForm.tsx`、`Admin.tsx`

2. **普通 JavaScript/TypeScript 文件**：使用 camelCase 或 kebab-case 命名。

- 示例：`index.tsx`、`localStorageUtil.ts`
