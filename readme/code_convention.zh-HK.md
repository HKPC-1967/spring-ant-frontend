## 建議遵循 Ant Design Pro 的程式碼規範。

## 檔案命名規範：

1. **React 元件檔案**：使用 PascalCase（大駝峰）命名。這樣可以幫助區分 React 元件與一般的 JavaScript 函式或變數。

- 範例：`LoginForm.tsx`、`Admin.tsx`

2. **一般 JavaScript/TypeScript 檔案**：使用 camelCase 或 kebab-case 命名。

- 範例：`index.tsx`、`localStorageUtil.ts`
